import React from "react";

const SearchBar = () => {
  return (
    <input
      type="text"
      name="search"
      className="search"
      placeholder="Search..."
    />
  );
};

export default SearchBar;
