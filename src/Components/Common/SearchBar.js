import React from "react";

const SearchBar = () => {
  return (
    <input
      type="text"
      name="search"
      className="search"
      placeholder="Search a user..."
    />
  );
};

export default SearchBar;
